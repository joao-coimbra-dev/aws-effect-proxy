{
  "name": "aws-effect-proxy",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "src/handler.js",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node24 --outfile=dist/index.mjs --format=esm --external:buffer --banner:js=\"import { createRequire } from 'module';const require = createRequire(import.meta.url);\" && esbuild src/users-handler.ts --bundle --platform=node --target=node24 --outfile=dist/users-handler.mjs --format=esm --external:buffer --banner:js=\"import { createRequire } from 'module';const require = createRequire(import.meta.url);\"",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "lint:type-aware": "oxlint --type-aware",
    "prepare": "effect-language-service patch",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.971.0",
    "@aws-sdk/util-dynamodb": "^3.971.0",
    "@effect-aws/client-dynamodb": "^1.10.6",
    "@effect-aws/lambda": "^1.5.1",
    "effect": "^3.19.14"
  },
  "devDependencies": {
    "@effect/language-service": "^0.65.0",
    "@types/aws-lambda": "^8.10.159",
    "@types/node": "^25.0.8",
    "esbuild": "^0.27.2",
    "oxfmt": "^0.24.0",
    "oxlint": "^1.39.0",
    "oxlint-tsgolint": "^0.11.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17"
  },
  "packageManager": "pnpm@10.27.0"
}
